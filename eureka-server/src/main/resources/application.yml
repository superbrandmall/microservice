server:
  port: 8761
logging:
  file: /home/mircoservice/log/eureka-server.log
eureka:
  #instance:
    #hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    #serviceUrl:
      #defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
  server:
    #开发时关闭自我保护
    enable-self-preservation: false